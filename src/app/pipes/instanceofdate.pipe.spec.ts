import { InstanceofdatePipe } from './instanceofdate.pipe';

describe('InstanceofdatePipe', () => {
  it('create an instance', () => {
    const pipe = new InstanceofdatePipe();
    expect(pipe).toBeTruthy();
  });
});
